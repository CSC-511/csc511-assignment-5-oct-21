import localStorage from 'csc511-assignment-4-october-7/utils/local-storage';
import { module, test } from 'qunit';

module('Unit | Utility | localStorage', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = localStorage();
    assert.ok(result);
  });
});
